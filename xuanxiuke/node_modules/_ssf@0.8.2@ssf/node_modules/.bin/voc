#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_voc@1.0.0@voc/voc.njs" "$@"
  ret=$?
else 
  node  "$basedir/../../../_voc@1.0.0@voc/voc.njs" "$@"
  ret=$?
fi
exit $ret
