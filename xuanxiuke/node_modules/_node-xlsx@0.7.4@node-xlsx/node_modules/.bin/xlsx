#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_xlsx@0.8.8@xlsx/bin/xlsx.njs" "$@"
  ret=$?
else 
  node  "$basedir/../../../_xlsx@0.8.8@xlsx/bin/xlsx.njs" "$@"
  ret=$?
fi
exit $ret
