#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_cfb@0.11.1@cfb/bin/cfb.njs" "$@"
  ret=$?
else 
  node  "$basedir/../../../_cfb@0.11.1@cfb/bin/cfb.njs" "$@"
  ret=$?
fi
exit $ret
